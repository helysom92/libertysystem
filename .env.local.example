NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: only needed for the "Comparar com serviços parecidos (IA)" button in the
# orçamento calculator. Get a key at https://console.anthropic.com — without it, the
# button just shows a friendly "not configured yet" message, nothing else breaks.
ANTHROPIC_API_KEY=your-anthropic-api-key
